import React, { ReactNode } from "react";
import { Icon } from "src/components/atoms/Icon";

export const GiftedScrollToBottom = (): ReactNode => {
  return (
    <Icon family="font-awesome" size={15} name="arrow-down" color="darkgray" />
  );
};
